Excellent progress. Now do a full end-to-end integration pass + final polish.

PHASE 3 — Integration, Polish & Demo Readiness:

1. CRITICAL FIXES FIRST:
- Run a full TypeScript check across both api-server and admin
- Fix any broken imports, missing types, undefined references
- Ensure every API route handler has proper error handling (try/catch → structured error response)
- Validate all Zod schemas match Drizzle schema exactly
- Fix any 500 errors on API routes

2. MEMBER FLOW — complete end-to-end:
- Create member → auto-generate member number (AURA-2026-XXXX)
- Assign plan → create membership → auto-generate invoice
- Record cash payment → invoice marks paid → timeline event logged
- Upload Baridimob proof → admin confirms → payment confirmed + audit log entry
- Generate QR code displayed in member profile (rotating JWT, shown as actual QR image in UI)
- Member freeze request → admin approves → membership status = frozen → auto-resume on end date

3. ACCESS CONTROL — complete flow:
- POST /api/access/verify accepts { token } → decode JWT → check membership active → check zone → check time rule → log result → return { allowed: boolean, reason?, member? }
- Kiosk demo page at /kiosk: full-screen QR scanner simulation — shows member photo, name, plan, green AUTORISÉ or red REFUSÉ with reason, auto-resets after 3 seconds
- Access point heartbeat: PATCH /api/access/points/:id/heartbeat updates last_heartbeat

4. BILLING — complete flow:
- Overdue detection: any invoice past due_date with status pending → mark overdue, show on dashboard
- PDF-ready invoice layout component (print-friendly, no PDF library needed — use @media print CSS)
- Daily reconciliation: GET /api/reports/reconciliation?date=YYYY-MM-DD → expected cash (all cash payments that day) vs manual close amount

5. CLASSES — complete flow:
- Waitlist auto-promotion: when booking cancelled → find first waitlist entry → create booking → mark waitlist entry notified → create notification record
- Attendance page for session: mark each booking as present/absent/late, save bulk
- No-show count shown on member profile

6. NOTIFICATIONS — wire triggers:
Wire notification record creation on these real events:
  - Membership created → membership_welcome
  - Payment confirmed → payment_confirmed  
  - Payment rejected → payment_rejected
  - Membership expiring in 3 days → membership_expiry_3days (run check in background job or on dashboard load)
  - Booking confirmed → booking_confirmed
  - Waitlist promoted → waitlist_promoted

7. REPORTS — make all charts real:
- Revenue chart: real data grouped by day/week/month with method breakdown
- Member growth: real new members per month for last 12 months
- Peak hours heatmap: access logs grouped by hour + day of week (7×24 grid)
- Class attendance rate: real bookings/capacity ratio per class type
- Store revenue: real order totals per product category

8. KIOSK PAGE (/kiosk):
Full-screen page, no sidebar, dark background:
- Large clock (current time)
- QR input field (simulate scan with text input)
- On verify: show member photo placeholder, name in Arabic + French, plan name, large green ✓ AUTORISÉ or red ✗ REFUSÉ
- Denial reason in Arabic + French
- Auto-reset after 3 seconds

9. MEMBER PORTAL PREVIEW (/portal/:memberNumber):
Simple read-only page showing:
- Member name, photo, membership status
- QR code (rotating, regenerates every 60s)
- Active membership details (plan, expiry, zones)
- Next class booking
- Points balance

10. FINAL SEED UPGRADE:
- Ensure all 50 members have complete realistic data
- Add Arabic names for all members (stored in name_ar field)
- 10 pending Baridimob payments with proof_url placeholders
- Access logs for today with realistic timestamps
- 3 expiring memberships (expires in 1, 3, 7 days)
- 2 overdue invoices
- Low stock on 3 products

11. FINAL CHECKLIST before marking complete:
□ Login works with admin@aurafitness.dz / Admin@2024!
□ Dashboard shows real data (not zeros)
□ Create a new member end-to-end works
□ QR code visible on member profile
□ Kiosk page /kiosk is accessible and functional
□ Access verify endpoint returns correct responses
□ Billing: record cash payment → invoice updates
□ Classes: book a session → capacity decreases
□ Reports: all charts show real data
□ Settings: audit log shows recent actions
□ TypeScript: zero errors on build

Run the checklist yourself, fix anything that fails, then give a final completion report with:
- Live URL
- Demo credentials
- Feature completion percentage per module
- Known limitations
- Suggested next steps for Phase 2 features